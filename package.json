{
  		"name": "create-users-lambda",
  		"version": "0.0.1",
  		"description": "AWS Lambda function to retrieve user list",
  		"main": "index.js",
  		"devDependencies": {
		    "aws-sdk": "latest",
    		"grunt": "0.4.*",
    		"grunt-aws-lambda": "0.8.0",
    		"npm": "^2.8.3"
  		},
  		"scripts": {
    		"test": "echo \"Error: no test specified\" && exit 1"
  		},
  		"author": "alexw"
	}
